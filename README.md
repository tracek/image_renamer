```bash
./renamer.py --help   
Usage: renamer.py [OPTIONS]

  Batch image renamer - from Q with love

Options:
  --path PATH         Path to images.  [required]
  --output TEXT       Output dir.
  --prefix TEXT       Optional prefix
  --dry               Dry run
  --date_format TEXT
  --help              Show this message and exit.
```

Example:
```bash
./renamer.py --path path_to_images --output Test
```
